name: Ordino Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
   runs-on: ubuntu-latest
   container:
     image: node:20-alpine
   steps:
    - name: Ordino Tests
      uses: ORDINO-AI-LLC/github-action@v3
